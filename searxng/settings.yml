use_default_settings: true

server:
  bind_address: '0.0.0.0'
  port: 8080
  secret_key: 'local-searxng-development-key-change-me'

search:
  formats:
    - html
    - json

engines:
  - name: wikidata
    disabled: true
